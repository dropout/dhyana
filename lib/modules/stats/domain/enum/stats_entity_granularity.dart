/// Enum representing the granularity of statistics entities.
/// Name of the enum values are used as bucket collection names in the database.
enum StatsEntityGranularity {
  days,
  weeks,
  months,
  years,
}
