import 'package:flutter/material.dart';

class TimerSettingsHistoryListItem extends StatelessWidget {
  const TimerSettingsHistoryListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
